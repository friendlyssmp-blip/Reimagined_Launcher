{
  "version": "1.0.13",
  "notes": "v1.0.13 — Fixed: the launcher now reopens by itself after installing an update. Root cause: the silent NSIS installer never shows its Finish page, so it never relaunched the app. The updater now arms a detached helper that waits for the installer to finish replacing the files and then starts the new launcher automatically.",
  "installer": "dist/Reimagined-Setup-1.0.13.exe"
}
