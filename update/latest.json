{
  "version": "1.0.8",
  "notes": "Settings → Updates cleanup: the 'Check for updates automatically' toggle is gone — update checking is now always on with no way to disable it, and the 'Auto-install updates on start' toggle no longer overlaps its description text.",
  "installer": "dist/Reimagined-Setup-1.0.8.exe"
}
