{
  "version": "1.0.5",
  "notes": "Fixes the installer crash 'Error opening file for writing: bg.bmp' (brand assets are now extracted to a valid temp directory) and makes the update check never freeze the wizard (hard curl timeout - the bundled version installs if the network stalls).",
  "installer": "dist/Reimagined-Setup-1.0.5.exe"
}
